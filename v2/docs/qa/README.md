# QA Folder

Purpose: Central location for manual QA run artifacts and evidence.

## Structure
- `evidence/` screenshots and media captured during QA.
- `runs/` per-run notes or exported checklist copies.

## Naming
- Evidence folder per run: `evidence/YYYY-MM-DD-<topic>/`
- Screenshot format: `qa-<area>-<resolution>.png`
